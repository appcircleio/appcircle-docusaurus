:::danger Nexus HTTPS Protocol

When configuring Sonatype Nexus, ensure that you use the `https` protocol for secure communication.  

Although Nexus can technically be accessed over `http`, it is **strongly recommended** to use `https`, especially when authentication is involved or when accessing private repositories.

:::