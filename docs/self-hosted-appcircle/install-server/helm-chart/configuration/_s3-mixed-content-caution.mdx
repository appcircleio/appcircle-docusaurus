:::caution
Keep in mind that, in order to use HTTP (`useHttp: "true"`) for the S3 endpoint, the Appcircle server should also be configured as HTTP.

Otherwise the browser might block HTTP requests because of the **[mixed content](https://developer.mozilla.org/en-US/docs/Web/Security/Mixed_content)**.
:::